---
import AuthLayout from "@/layouts/AuthLayout.astro";
import ResetPasswordForm from "@/components/auth/ResetPasswordForm";
import Providers from "@/components/Providers";

// Server-side rendering disabled to allow client-side token handling
// Supabase handles password reset tokens via URL hash (#access_token=...)
// which is only accessible on the client side
export const prerender = false;
---

<AuthLayout title="Nowe hasło - 10xWordUp">
  <Providers client:only="react">
    <ResetPasswordForm client:only="react" />
  </Providers>
</AuthLayout>
